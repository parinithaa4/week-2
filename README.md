# week-2
I built a Convolutional Neural Network (CNN) model to classify garbage images into six categories: cardboard, glass, metal, paper, plastic, and trash. The project includes dataset preprocessing, model training, evaluation, and saving the trained model.
